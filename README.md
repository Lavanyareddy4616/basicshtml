GIT MERGE concept, men and women branch

women brnach in merged into men, to check merge concept

when git merge women from men branch means- 
all the files from women is merged into men, 
if women has w1 w2 w3 files and men has m1 m2 m3 files- after merging from men branch - git merge women
after meging---> men branch has - w1 m2 w2 m2 w3 m3 files
            ---> women branch has - w1 w2 w3 files

* merging can be between any two any brnach not only with main branch
* merging can also happen in local, but there will be restrictions when merged to main branch
* merge can happen even when there is conflict with the data inside the files, not only with the files
  
