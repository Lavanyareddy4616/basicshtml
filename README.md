git rebase girls

git rebase is done when there is no impact of commit history
git merge vs git rebase is interiew question
git rebase is also similiar to git merge
but in git merge, commits are done in order and in git rebase commits gets inserted to another branch commits
